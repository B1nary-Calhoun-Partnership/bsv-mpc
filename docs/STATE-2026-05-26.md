# bsv-mpc — Project State Synthesis (2026-05-26)

> Swarm output (2 parallel synthesis agents: done/proven + open/next), for graphify + next-best-steps.
> Org note: **Calhooon** = org; **Calgooon** = personal (LIVE relay deploys from Calgooon/rust-message-box; bsv-rs crate Calgooon-owned). Repo: `B1nary-Calhoun-Partnership/bsv-mpc`.

---

## A. MAINNET-PROVEN CAPABILITIES (entity → issue/PR → TXID → proves)

- **True-loss recovery** — #40 (CLOSED), PR #57+#59 (MERGED) — TXID `f8b51458…` — device-loss recovery; joint key `02613431…`=K, **address UNCHANGED across DKG + 2 reshares**; recovered device `{0,2}` presign+sign over relay spends K's UTXO. (LANDED 2026-05-26.)
- **Policy/approval gate** — #43 (CLOSED), PR #50 (MERGED) — TXID `7ada3f9d…` — what-you-see-is-what-you-sign approval enforced before signing material consumed.
- **N-party device-holds 4-of-6** — #38 (CLOSED), PR #46 (MERGED) — TXID `febd2877…` — t−1 device shares + 1 external = sig; device-alone rejected. No new crypto.
- **Presign-over-relay** (deployed container) — #40/#58, PR #59 — TXID `550d79df…` — §06.17.1 PresigBundle over relay → cosign → verify → spend.
- **Reshare-over-relay** — #40/#58, PR #59 — TXID `6c0f17cc…` — first deployed reshare over relay; key unchanged.
- **Cross-(t,n) address-preserving reshare** — #35 (CLOSED) — TXID `5137b913…`.
- **HD relay sign (BRC-42 §06.20)** — #26 (CLOSED) — mainnet child-key TXID (commit `49a4071`).
- **Multi-input retire-legacy sign** — #13 (CLOSED), PR #36 — TXIDs `14c8189f…` + `793938e3…` — legacy 4-round HTTP deleted; relay-only sign.
- **Key refresh + presig invalidation (§18.2)** — #10/#22 (CLOSED) — TXID `db865f22…`.
- **2-of-3 DKG + sign over MessageBox (M1 baseline)** — PR #19/#20 (MERGED).

## B. GOD-TIER TRACK (#37 umbrella, OPEN until children done)
- #38 §1 device-holds-(t−1) — DONE+proven. #39 §4 approval-binding audit — DONE (reconciled). #40 §3 recovery — DONE+proven. #43 §4 policy+approval — DONE+proven. #44 zeroize — DONE.
- **#41 native client foundation — OPEN, the main remaining child.** Realizable, known patterns; ~25–35 eng-wks; 3 shells (iOS/Android/web). Net-new: enclave wrap-key, WebAuthn-PRF backup, UniFFI/JS bindings, zeroize (core done via #44). Gate = first shell signs mainnet tx with biometric-gated enclave-wrapped share. 100% Calhoun-solo.
- Seams (OPEN, not blockers): #55 export joint pubkey from keygen-only share (instant-signup; tiny accessor); #56 concurrent multi-device sessions (design; option B uses #40 reshare primitive).

## C. INFRA/TRANSPORT (#58 chain — CLOSED, all merged+released)
- **Container OOM** (back-to-back 2048-bit Paillier safe-prime gen > 12GiB, no swap, standard-4=max) → `MALLOC_ARENA_MAX=2` + `paillier_pool::generate_serialized` + `worker.js` readiness barrier (`startAndWaitForPorts`). PR #59 (MERGED).
- **bsv-rs BRC-104 transport hang** (no-timeout reqwest, CF egress drops idle keep-alive) → `timeout(30s)`+`connect_timeout(10s)`+`pool_max_idle_per_host(0)` → **bsv-rs v0.3.13** (Calhooon/bsv-rs PR #5/#6, RELEASED). bsv-mpc now pins published 0.3.13 (PR #60, dropped git patch).
- **Relay 128MB Worker OOM** (unbounded `listMessages` SELECT + never-acked boxes; CF 1102) → `LIMIT 100` NO `ORDER BY` + canonical **acknowledge-on-consume** (`ack_best_effort` in `messagebox.rs`) → Calgooon/rust-message-box v0.2.1 (PR #63/#64) + bsv-messagebox-cloudflare mb-v0.2.1 (PR #3/#4), RELEASED.
- **WS-pump bound** (`subscribe.rs` BACKFILL_TIMEOUT — dead HTTP can't block WS pump; the decisive presign fix) + **presign reorder to WS live-push** + **pending_inbound buffer** + **position↔absolute index translation** (§05.4.6) — PR #59 (MERGED).
- **Open PRs: NONE. Uncommitted: NONE.** Everything merged. (rustfmt CI red is PRE-EXISTING, not from these.)

## D. OPEN ISSUES
- **#41** native client (step:implement) — biggest remaining frontier.
- **#37** umbrella (open until #41).
- **#5** production hardening backlog (pre-funded-mainnet-at-scale): KSS handler-level BRC-31 authz still TODO; at-rest encryption of seed-primes blob in DO; document share-key blast radius; rate-limiting/DoS on KSS; benchmark wasm32 sign latency on deployed worker; version-control wrangler.toml; de-POC cosigner DO; converge HTTP-bundle vs relay-MessageEnvelope framings.
- **#2** v1.0 CF-native cosigner umbrella (~88%): Phase J (CHIP + /capabilities + /health.json, §16), Phase K (cross-stack joint mainnet TX with rust-mpc — closes MPC-Spec #36; BLOCKED on rust-mpc deploy).
- **#56** concurrent multi-device (design), **#55** keygen-only pubkey export (small).

## E. REMAINING PRODUCT GAPS (small, post-#40)
- Recovery **settle band-aid → god-tier container readiness gate** (release reshare listener on commit; not blocking — recovery proven with the band-aid).
- **signing.rs full-4-round non-contiguous index follow-up** (latent same-class bug as the PR#59 presign fix; sign-relay routes by identity hex so not yet hit).
- **File MPC-Spec §05.4.6/§05.5.3 clarification** (from_party/to_party = ABSOLUTE keygen index; impls MUST translate SM-position→absolute before wire).
- Decide debug endpoints keep-behind-flag vs strip; verify speculative IPv4-prefer/polling-fallback reverted in #59.

## F. SPEC DIRECTION + OPEN QUESTIONS
- ~30+ open Qs (Q15–Q60: UX/cost/security/speed/quality). 12 design choices need user steering (CHANGES-PROPOSED.md). ADRs 0031–0049 all PROPOSED; M1-critical = ADR-0032 (approval view_hash), ADR-0037 (CBOR re-encode equivalence), ADR-0044 (wallet-renderer canonicalization, unblocks ADR-0032).
- **Deferred:** Q14 AuthSocket crate; micropayment routing model (not blocking demo — runs free); TEE/HSM v2; Cedar DSL v2.
- **Wallet-MVP realizability RESOLVED:** mandatory-external = flat t-of-n via `t=w+1`; **4-of-6 chosen** (w=3, second={Notary A, Notary B, cold device}); 2-tier is optional v2. (Corrects stale MEMORY line "needs 2-tier/nested".) Only crypto lift = device-holds-(t−1) DKG = PROVEN (#38).
- **Platform reality (gates #41):** secp256k1 NOT in Secure Enclave/StrongBox (P-256 only) → wrap-key pattern (enclave KEK unseals share into app memory per signature; transient exposure window). WebAuthn-PRF real but flaky over passkey sync; cross-ecosystem L1 recovery → trustees.

## G. MILESTONES + DATES (today = 2026-05-26)
- **M1 cross-impl signing demo — 2026-05-29 (3 DAYS OUT, IMMINENT).** 1 bsv-mpc + 2 rust-mpc → 2-of-3 DKG → verified mainnet sig + shared BRC-22 audit topic. bsv-mpc side de-risked (own TXIDs landing); **AT RISK on the cross-stack joint leg (Phase K) — depends on Binary's rust-mpc deploy + conformance vectors green on both stacks.**
- **Phase 0 sign-off — 2026-06-12** (slipped from 05-22): locks §01-05 crypto foundation; absorbs ADR-0032/0037. Until locked, no joint ceremony has a verified mainnet sig.
- **M2 / 30-day Notary MVP wedge — ~2026-06-12** (concurrent): specialty cosigner, public policy posture, overlay-discoverable, per-sig monetized. P1, smallest revenue surface.
- **2-week cross-impl signing test** = M1 shape (the gating P0).

## H. CROSS-IMPL CONVERGENCE (vendor-neutral network)
- Two parallel Rust stacks meeting ONLY at MPC-Spec wire + cggmp21-fork: Calhoun (bsv-mpc + bsv-rs v0.3.13 + Calgooon/rust-message-box CF Worker) ↔ Binary (rust-mpc + bsv-sdk v0.2.x + Binary message-box on Railway).
- Shared: MPC-Spec (public) + cggmp21-fork (`6c6421ee`, both pin same commit, only adds set_additive_shift).
- **Needed for interop:** conformance vectors round-trip BOTH stacks (BRC-42 §03, envelope §05, ADR-0030 presig §06.16); 4 byte-locked vectors (09-rendered-text sha256 8c383828 byte-identical); canonical-TS @bsv/message-box-client v2.0.7 (Socket.IO + BRC-103); CBOR byte-equivalence (ADR-0037); from_party/to_party=absolute (§05.4.6); both pin past CVE-2025-66016/66017; **Phase K joint TXID BLOCKED on rust-mpc deploy (Binary)**.

## I. DEPENDENCIES / BLOCKING (graph edges)
- M1 (05-29) ← cross-impl interop ← rust-mpc deployed + conformance green both stacks + Phase K joint TXID ← **Binary's rust-mpc deploy** (gating unknown; bsv-mpc side de-risked).
- Phase K ← rust-mpc canonical + deploy (Binary).
- Notary MVP (M2) ← Phase 0 lock + overlay discovery (Phase J) + per-sig fee; micropayment DEFERRED.
- #41 native client ← enclave wrap-key + WebAuthn-PRF + UniFFI + zeroize(core done).
- #56 ← #40 reshare primitive (DONE) for option B.
- #5 hardening ← blocks funded-mainnet-AT-SCALE, not M1/M2 demos.
- Scaling (10K→1M) ← per-session DO routing + DO-SQLite coordinator-state resume + safe-prime factory + (≥100K) overlay operator sharding. Hard limit: 12GiB/no-swap per instance → horizontal only.

## J. STALE-SOURCE CORRECTIONS (for the graph)
1. MEMORY/HANDOFF §0b "BLOCKED/in-flight" #40 → SUPERSEDED: #40 DONE/mainnet-proven (project_40_recovery_landed, issue CLOSED).
2. "wallet realizability open / needs 2-tier" → SUPERSEDED: flat t-of-n RESOLVED (4-of-6).
3. "approval-binding ZERO implemented" → STALE: shipped + mainnet-proven (#43).
