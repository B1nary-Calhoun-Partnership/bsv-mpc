# Session handoff — 2026-05-19

> For the next Claude session picking up the v1.0 CF-native cosigner
> work cold. Read this + `docs/NEXT-STEPS.md` first; then ask the user
> about Q1-Q8 before doing anything else.

## TL;DR — the next concrete action

**Phase G Step 1 — Investigation** (per `docs/NEXT-STEPS.md`).

That step is: spawn 2-3 parallel Explore agents to survey:
1. `round_based` crate source — any async SM variant we missed? upstream cggmp21 fork activity toward async?
2. `~/bsv/mpc/bsv-mpc/poc/poc2-wasm-compile/` — what did POC 2 *actually* compile + run?
3. `~/bsv/agents/` (the 11 production CF Workers) — any single-threaded async-only Rust patterns worth borrowing?
4. cggmp24's `round_based::StateMachine` — `!Send` constraint, async variants?

Step 1 produces no commit. Step 2 produces `docs/PHASE-G-AUDIT.md` (first phase-G commit, doc-only). Step 3 produces `poc/poc16-sm-async/` (POC commit, proves the unknown). Step 4 produces implementation commits. Step 5 produces the merge gate (existing Phase D + E mainnet TXID shape unchanged + new wasm32 single-threaded executor test passes 2-of-2 DKG sim).

**Do NOT skip to implementation.** The 5-step workflow per phase is the locked discipline — see `docs/NEXT-STEPS.md` "Workflow discipline" section.

## What's done as of this handoff

### Shipped on `bsv-mpc` main

| Commit | What |
|---|---|
| `83612b1` Phase A | Canonical RoundMessage ↔ MessageEnvelope wrap/unwrap helpers in `bsv-mpc-core` |
| `1dbcf2c` Phase B | Typed `send_round_message` / `subscribe_round_messages` on `MessageBoxClient` |
| `5c2110a` test fix | `TEST_LOCK` mutex for env-mutating proxy config tests |
| `067ce2a` Phase C | `MessageBoxListener` dispatcher primitive in `bsv-mpc-service` |
| `3f4865c` Phase D | `DkgHandler` + 2-of-2 DKG via MessageBox e2e (byte-identical joint pubkey) |
| `b18d2a6` Phase E code | `SigningHandler` + mainnet TX harness |
| `ed7feaf` Origin fix + docs | `Origin: http://admin.com` + `docs/WALLET-3321.md` |
| **`e135ae0` plan** | **`docs/NEXT-STEPS.md` — phased plan for v1.0 CF-native cosigner** |

### Mainnet artifact

**TXID [`82ccb15c49985a32b355a618f417bb7a09ec4ee5cf34e539e9baaebb74dadc29`](https://whatsonchain.com/tx/82ccb15c49985a32b355a618f417bb7a09ec4ee5cf34e539e9baaebb74dadc29)** — within-stack 2-of-2 MessageBox-signed mainnet TX. The bar for every future quality gate.

### Issue tracking

- **bsv-mpc milestone #1** — "v1.0 — CF-native cosigner (Calhoun-side)" → https://github.com/B1nary-Calhoun-Partnership/bsv-mpc/milestone/1
- **bsv-mpc#2** — umbrella tracker (labels: `umbrella`, `wire-compat`, `security`) → https://github.com/B1nary-Calhoun-Partnership/bsv-mpc/issues/2
- **Org project Partnership Roadmap** — bsv-mpc#2 added, status Todo
- **MPC-Spec #36** — joint cross-stack TXID issue, cross-linked from bsv-mpc#2 via comment
- **MPC-Spec #2** — Calhoun-side MessageBox transport — closed with Phase E TXID as closing artifact

### Labels created on bsv-mpc

- `phase:G` / `phase:H` / `phase:I` / `phase:J` / `phase:K` — phase tag
- `step:investigate` / `step:document` / `step:poc` / `step:implement` / `step:gate` — workflow stage
- `wire-compat`, `security`, `cleanup`, `umbrella` — categories

## Locked decisions (do not re-litigate without explicit user redirect)

1. **Approach (A)** — full CF-native: `bsv-mpc-worker` Cloudflare Worker + Durable Object holding outbound WS to the MessageBox relay. Three audit RED findings treated as engineering work, not blockers.
2. **5-step workflow per phase** — investigate → document (audit doc commit) → POC (poc/poc<N>-<name>/ commit) → implement → quality gate. Each step lands on main BEFORE the next begins.
3. **110% / no asterisks** — every phase gate produces either a byte-exact / on-chain / verifiable third-party artifact, or the phase stays open. No "should work" framing.
4. **Spec interop** — implementation conforms to MPC-Spec, never the inverse. Per `feedback_canonical_ts_immutable` + `feedback_spec_first_then_propose`.
5. **Issues on bsv-mpc, joint stuff on MPC-Spec** — impl details co-locate with code; joint deliverables (e.g., #36) stay on the partnership repo.
6. **POCs in `poc/poc<N>-<name>/`** — continues the existing 15-POC convention (poc1-15 already shipped). Next POCs: poc16 (G) / poc17 (H) / poc18 (I) / poc19 (J) / joint POC (K).
7. **Deadline-flexible** — user explicitly said M1's 2026-05-29 deadline doesn't matter, push back months if needed. UX + security + spec interop > speed.

## Q1-Q8 still need user answers

Defaults are documented in `docs/NEXT-STEPS.md`. The user can say "use defaults" or pick each:

| Q | Topic | Default |
|---|---|---|
| Q1 | G + H parallel or serial | Serial |
| Q2 | SM rewrite LocalSet-only or runtime-trait abstraction | LocalSet-only (KISS) |
| Q3 | Per-identity Worker vs multi-tenant Worker+DO | Per-identity |
| Q4 | Fold Phase J into I, or separate phase | Separate (smaller commits) |
| Q5 | Interim (B) deployment while building (A) | Skip (avoid throwaway) |
| Q6 | Issue hierarchy: 5 phase + umbrella tracker, or other shape | 5 phase + umbrella (current scaffold) |
| Q7 | New milestone vs extend M1 | New milestone (already created) |
| Q8 | Auto-add bsv-mpc to org project | Manual for now (already added umbrella manually) |

**Ask the user for these before starting Phase G Step 1** — particularly Q1 (parallel vs serial) since it affects task scheduling.

## Critical references in this order

1. **`docs/NEXT-STEPS.md`** — the canonical plan. Read first.
2. **`docs/WALLET-3321.md`** — wallet:3321 admin Origin + canonical request shapes. Needed for any mainnet test that funds via local wallet.
3. **`CLAUDE.md`** (root) — project-level rules (BRC standards, dep conventions, all-Rust constraint).
4. **`crates/bsv-mpc-core/CLAUDE.md`** — core crate's own CLAUDE.md.
5. **MPC-Spec `~/bsv/mpc/MPC-Spec/STATUS.md`** — partnership state, who's doing what.
6. **MPC-Spec `~/bsv/mpc/MPC-Spec/06-transport.md`** — §06.14 normative ("MUST add MessageBox transport client").
7. **MPC-Spec `~/bsv/mpc/MPC-Spec/16-operations.md`** + `decisions/0016-v1-ops-topology-no-tee-no-hsm.md` — what spec-conformant deployment requires.

## Discipline lock — do NOT do these

- ❌ Do not commit code without unit + vector + (where applicable) e2e tests green.
- ❌ Do not skip a phase's audit doc step. The doc lands on main BEFORE the POC + implementation.
- ❌ Do not skip a phase's POC step. The POC ships as its own commit BEFORE implementation begins.
- ❌ Do not propose changes to MPC-Spec to make implementation easier. The implementation conforms to the spec.
- ❌ Do not run `cargo fmt --skip` or `--no-verify` or any other gate-bypass without user explicit OK.
- ❌ Do not commit on `bsv-mpc-old-unscrubbed/` — always `cd ~/bsv/mpc/bsv-mpc/`. The user's actual partnership remote is the `B1nary-Calhoun-Partnership/bsv-mpc` repo; `bsv-mpc-old-unscrubbed` is a pre-scrub local copy.
- ❌ Do not push to MPC-Spec / bsv-mpc main without verifying CI history is clean (`gh run list --branch main --limit 5`).
- ❌ Do not bundle unrelated changes in a single commit.

## Things the user has been explicit about (memory-load these on session start)

- **"god-tier only, full-stack awareness"** — consult `~/bsv/` Rust + TS reference stack before proposing fixes; never recommend pragmatic-today / suppress / defer when a real fix is reachable.
- **"gate every change hard, prove it works"** — unit / e2e / conformance proof required before merge, every time.
- **"e2e with real sats"** — wire-format / crypto / on-chain / cross-impl changes must be gated by real-mainnet evidence.
- **Origin for wallet:3321 = `http://admin.com`** — see `docs/WALLET-3321.md`. Local wallet has plenty of sats.
- **"spec-first, then propose"** — read the relevant MPC-Spec § section in full before planning implementation.
- **No peer sign-off on impl-only PRs** — Calhoun-side bsv-mpc PRs that don't touch MPC-Spec don't need Mitch/Binary review; conformance vectors + live-relay proof are the merge gate.

## What I am NOT doing in this handoff

- No code edits
- No new commits beyond this handoff doc
- No phase issues filed
- No audit docs drafted
- No POCs created
- No project board changes beyond what's already done (bsv-mpc#2 added with Todo status)

The next session is fully free to start Phase G Step 1 cleanly, with all the necessary context in `docs/NEXT-STEPS.md` + this handoff + the locked decisions above.

---

**Open MessageBox relay (live):** `https://rust-message-box.dev-a3e.workers.dev`
**Local wallet (mainnet sats source):** `http://localhost:3321` with `Origin: http://admin.com`
**Test command for re-running Phase E within-stack:** see `crates/bsv-mpc-service/tests/sign_mainnet_via_messagebox_e2e.rs` header doc-comment.
